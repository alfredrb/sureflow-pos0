{
  "name": "Operator",
  "type": "object",
  "properties": {
    "operator_id": {
      "type": "string",
      "description": "Unique operator login ID"
    },
    "full_name": {
      "type": "string"
    },
    "pin": {
      "type": "string",
      "description": "Login PIN"
    },
    "role": {
      "type": "string",
      "enum": [
        "cashier",
        "csm",
        "manager",
        "technician",
        "loss_prevention"
      ],
      "default": "cashier",
      "description": "Technician role: POS access in locked Training Mode with diagnostics tools; admin access limited to Registers, Network, Hardware Status, and Maintenance Log. Loss Prevention role: admin-only access to logs, transactions, and the Loss Prevention workbench; no POS access."
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive"
      ],
      "default": "active"
    },
    "email": {
      "type": "string"
    },
    "store_id": {
      "type": "string",
      "description": "Store number this operator is assigned to. Blank = unassigned (visible centrally)."
    },
    "pos_access": {
      "type": "boolean",
      "default": true,
      "description": "If false, this operator cannot log into the POS or approve POS overrides."
    }
  },
  "required": [
    "operator_id",
    "full_name",
    "pin",
    "role"
  ]
}